# Smithery configuration: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio

configSchema:
  type: object
  properties:
    repoPath:
      type: string
      description: Path to the repository to analyze

commandFunction:
  |-
  config => ({ command: 'tempograph-server', args: [], env: {} })
